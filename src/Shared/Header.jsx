import React, { useState } from 'react';
//import Hamburger from './Shared/Hamburger.jsx';

const Header = ({menu}) => {
  return (
    <div className="border border-black w-full">Header Here</div>
  );
};

export default Header;
